namespace PresenceBackend.Models.Database;

/// <summary>
/// All user roles that a user can have
/// </summary>
public enum UserRole
{
    Admin = 1,
    Member = 2,
    KeyUser = 3,
    Observer = 4,
}